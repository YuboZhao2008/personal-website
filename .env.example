# Optional public origin, read at build time. No API credentials are needed.
# Leave blank for local development or until the production URL is confirmed.
# Set to the stable HTTPS origin, with no path, query, fragment, or credentials.
# When blank, canonical URLs and the sitemap entry are omitted; the site still runs.
SITE_URL=
